<header>
    <nav class="container-fluid">
        <div class="row">
            <div class="col-sm-2">
                <a href="/"><img src="{{ asset('img/logo.png') }}" /></a>
            </div>
            {{ render(controller('App\\Controller\\VehiculeController::searchBarAction')) }}
            <div class="col-sm-4">
                <ul>
                    <li><a href="{{ path('shop') }}"><span>Shop</span> <i class="fa fa-shopping-bag" aria-hidden="true"></i></a></li>
                    <li class=" dropdown">
                        <a class="dropdown-toggle" data-toggle="dropdown"id="dropdownMenuLink"  aria-haspopup="true" aria-expanded="false">
                            <span>{{ app.user.firstname|capitalize }} {{ app.user.lastname|capitalize }}<i class="fa fa-user" aria-hidden="true"></i></span>
                        </a>
                        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdownMenuLink">
                            <a class="dropdown-item" href="{{ path('profil', {'id' : app.user.id }) }}">Profil</a>
                            <a class="dropdown-item" href="#">Commandes</a>
                            {% if is_granted('ROLE_ADMIN') %}
                                <a class="dropdown-item" href="{{ path('dashboard') }}">Administration</a>
                            {% endif %}
                            <div class="dropdown-divider"></div>
                            <button type="button" onclick="$(location).attr('href', '/logout')" class="btn btn-danger">Déconnexion</button>
                        </div>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
</header>