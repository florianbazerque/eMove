
<div class="col-sm-6">
    {{ form_start(form) }}
    {{ form_errors(form) }}
    <div class="col-sm-10">
        <div class="form-group row">
            {{ form_errors(form) }}
            {{ form_widget(form.modele) }}
            {{ form_end(form) }}
        </div>
    </div>
    <div class="col-sm-1">
    </div>
</div>